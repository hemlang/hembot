name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libcurl4-openssl-dev \
            libssl-dev \
            uuid-dev \
            libsqlite3-dev \
            libffi-dev \
            libpcre2-dev \
            zlib1g-dev
          # Optional deps — continue if unavailable
          sudo apt-get install -y liblws-dev libusearch-dev || true

      - name: Build and install Hemlock from source
        run: |
          git clone --depth 1 https://github.com/hemlang/hemlock.git /tmp/hemlock
          cd /tmp/hemlock
          make -j"$(nproc)"
          # `make install` places the binary in /usr/local/bin and the stdlib
          # in /usr/local/lib/hemlock/stdlib — the @stdlib alias resolver
          # needs the latter or it won't find things like @stdlib/testing.
          sudo make install
          hemlock --version

      - name: Run all test suites
        run: make test

      - name: Compile check with hemlockc
        run: make build

      - name: Smoke test — agent parses and starts
        run: |
          # Pipe empty input so it exits on EOF; we just want to verify it parses.
          printf '' | timeout 5 hemlock src/hembot.hml || [ $? -eq 124 ] || exit 1
